name: CI

# Quality gate on pull requests. Deploys never run here: they happen in
# deploy.yml, only after a merge to main (and only for changed services).

on:
  pull_request:
    branches: [main, develop]

jobs:
  tests:
    uses: ./.github/workflows/tests.yml
